# Deploy both services from GitHub: https://dashboard.render.com/select-repo?type=blueprint
services:
  - type: web
    name: gwf-api
    runtime: docker
    plan: free
    rootDir: backend/Gwf.Api
    dockerfilePath: ./backend/Gwf.Api/Dockerfile
    dockerContext: ./backend/Gwf.Api
    healthCheckPath: /api/products
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: AllowedOrigins
        value: https://gwf-web.onrender.com

  - type: web
    name: gwf-web
    runtime: static
    plan: free
    rootDir: frontend/gwf-web
    buildCommand: npm install && npm run build:render
    staticPublishPath: dist/gwf-web/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: API_HOST
        fromService:
          type: web
          name: gwf-api
          property: host
